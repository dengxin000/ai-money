# 双色球选号系统优化时间线

## 0. 初始版本

初始目标：
- 输入近 20 期号码
- 输出 3-5 组推荐号码
- 给出模型评分和相对权重

初始逻辑：
- 统计红球热度、遗漏、区间、奇偶、大小、和值、连号、重号
- 统计蓝球热度和遗漏
- 红球池默认约 16 个
- 固定 2-3 个胆码
- 从红球池中组合补足 6 个红球
- 按结构规则过滤，再按评分排序

初始偏向：
- 偏均衡型
- 偏热号和中遗漏号
- 偏 3 区覆盖
- 偏 3:3 或 4:2 结构

主要问题：
- 胆码太硬
- 组合相似度高
- 蓝球偏长遗漏
- 对偏态结构适应不足

## 1. 第一次复盘优化

触发号码：
```text
02 05 14 25 30 32 + 05
```

当时推荐表现：
- 红球池命中 `02、05、30`
- 推荐组合多数只命中 `02、30`
- 蓝球 `05` 未进入最终推荐

发现的问题：
- 红球池偏窄，`14、25、32` 这类温号被挤掉
- 3 个硬胆码导致组合过度集中
- 和值过滤偏保守
- 蓝球过度偏向长遗漏
- 输出结果相似度高

调整动作：
- 红球池从 16 个扩大到 20 个
- 硬胆码从 3 个减少到 2 个
- 和值过滤从 `均值 ± 1.5 标准差` 放宽到 `均值 ± 2.5 标准差`
- 蓝球池改为长遗漏、近期温号、最近遗漏混合
- 输出层增加多样性约束

## 2. 第二次复盘优化

触发号码：
```text
04 06 10 18 23 31 + 11
```

发现的问题：
- 输入顺序被误解，用户数据实际是“最新在上”
- 红球组合对中高区补位仍然不够
- 硬胆码虽然减少，但仍会让结果黏住少数号码
- 蓝球 `11` 属于回补型信号，应进入考虑范围

调整动作：
- 页面新增“数据顺序”参数
- 默认改为“最新在上”
- 红球池增加温遗漏权重
- 中高区补位权重上调
- 胆码从 2 个进一步减少为 1 个核心胆
- 和值窗口再略微放宽

## 3. 第三次复盘优化

触发号码：
```text
06 07 11 18 22 33 + 05
```

当时推荐表现：
- 多组命中 `06、07、18、33` 中的部分号码
- 蓝球 `05` 被模型池覆盖
- 但 `11、22` 这种中区回补号没有被充分重视

发现的问题：
- 模型仍偏爱少数老权重号
- 中区号只是加分项，不是必要约束
- 一些组合容易偏向高区或固定热号

调整动作：
- 红球组合必须至少包含 1 个中区号
- 中区回补号增加权重
- 高频黏性号增加惩罚
- 中区覆盖从加分项提高为准入项

## 4. 第四次复盘优化

触发号码：
```text
05 18 23 24 27 33 + 03
```

当时推荐表现：
- 抓住 `18、27`
- 蓝球 `03` 在池中
- 但 `23、24、33` 的整体高区抬升没有处理好

发现的问题：
- 程序只知道“高区多”，但不知道高区内部层次
- 高区被整体处理，导致 `23-27` 和 `29-33` 混在一起
- 单期高和值后容易追高过度

调整动作：
- 新增高区冲高分支
- 高和值、高区多时，允许高区密度更高的组合
- 输出结果增加 `push` / `pull` 模式标记

后续暴露的问题：
- 冲高分支过强
- 容易过度抬 `29、30、32、33`

## 5. 第五次复盘优化

触发号码：
```text
02 04 15 23 25 27 + 03
```

发现的问题：
- 上一期高和值后，下一期没有继续冲顶，而是回落到中高带
- 真正落点在 `23-27`
- 程序却仍然偏向 `29-33`

调整动作：
- 将高区拆成两层：
  - `23-27`：中高回落带
  - `29-33`：高区冲顶带
- 没有连续两期高和值时，优先 `23-27`
- 连续两期高和值时，才提高 `29-33` 权重
- 冲高分支保留，但先判断高和值连续性

当前模式：
- `push`：连续高和值，偏 `29-33`
- `pull`：和值回落或未连续高和值，偏 `23-27`

## 6. 最新复盘结论

触发号码：
```text
02 13 14 16 20 24 + 05
```

当时推荐表现：
- 推荐中只命中少量 `13、14、24`
- 蓝球 `05` 在池中，但没有进入最终 5 组

这期结构：
- 和值：89
- 奇偶：1:5
- 大小：4:2
- 三区：1 / 4 / 1
- 核心特征：中区密集、偶数强、小号偏多

暴露的新问题：
- `pull` 模式仍然偏向 `23-27`，但这期直接回到中区密集
- 单胆码 `18` 仍然过硬
- 奇偶规则对 `1:5` 偶数强势结构不够友好
- 蓝球池覆盖了 `05`，但输出层没有保底展示

建议下一步优化：
- 新增“中区密集模式”
- 胆码改为“候选胆码池”，每组命中其一即可，不再强制同一个胆码
- 允许一组小概率分支使用 `1:5` 或 `5:1` 奇偶
- 蓝球近期热号至少出现在 1 组推荐中

## 7. 当前算法状态

当前已实现：
- 最新在上输入顺序
- 20 期滚动窗口
- 红球热度、遗漏、区间、和值、奇偶、大小评分
- 蓝球热度、遗漏、近期温号混合池
- 红球池 20 个
- 1 个核心胆码
- 中区准入
- 高频黏性惩罚
- 高区分层：`23-27` / `29-33`
- `push` / `pull` 两种高区策略
- 输出多样性约束

当前未实现但建议做：
- 中区密集模式
- 胆码池替代强制胆码
- 偶数强势分支
- 蓝球保底覆盖
- 历史回测统计每次优化是否有效

## 8. 总体演进

系统从最初的“均衡型选号器”，逐步演进为：

```text
均衡结构
  -> 扩池和降胆码
  -> 最新在上顺序修正
  -> 中区准入
  -> 高区冲高
  -> 高区分层
  -> 下一步：中区密集和极端奇偶分支
```

注意：
- 所有评分都是模型内部相对评分
- 不代表真实中奖概率提升
- 每次优化只是在减少上一轮暴露出的结构偏差

## 9. 稳定化升级计划落地

本次升级目标：
- 不再用单期开奖结果直接覆盖主策略
- 保留多个策略版本
- 用滚动回测评价策略稳定性

已落地功能：
- 页面新增“回测分析”标签
- 内置 `v1-v5` 策略版本
- 支持 21 期以上历史数据滚动回测
- 每次回测用前 20 期预测下一期
- 输出每个策略的：
  - 平均最佳红球命中
  - 平均每组红球命中
  - 3 红以上次数
  - 4 红以上次数
  - 蓝球命中次数
  - 综合分
- 自动标记长期最稳、近期最好、蓝球最好策略

下一步建议：
- 做“集成推荐”，由多个策略共同出号
- 保存每次推荐结果，方便未来和实际开奖号自动对照
- 新增 `v6_mid_dense` 中区密集策略
- 新增 `v7_even_bias` 偶数强势策略
